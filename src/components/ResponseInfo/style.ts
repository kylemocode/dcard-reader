import styled from 'styled-components';

export const S = {
	ResponseContainer: styled.div`
		display: flex;
		align-items: center;
		font-size: 12px;
	`,
	ResponseImg: styled.img`
		width: 55px;
		height: 25px;
		margin-right: 5px;

	`,
	ResponseCount: styled.p`
		margin-right: 10px;
		color: rgba(0,0,0,0.5);

	`,
	CommentTitle: styled.p`
		margin-right: 5px;
		color: rgba(0,0,0,0.5);
	`,
	CommentCount: styled.p`
		margin-right: 5px;
		color: rgba(0,0,0,0.5);
	`
}